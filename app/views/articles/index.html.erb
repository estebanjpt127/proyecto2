<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <meta name="mobile-web-app-capable" content="yes"> <!-- Meta actualizada para aplicaciones móviles -->
    <title>Blog</title>
    <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Lato:400,700,400italic,700italic" rel="stylesheet" type="text/css">
</head>

<body id="page-top">
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
        <div class="container">
            <a class="navbar-brand js-scroll-trigger" href="#">Blog SAAS</a>
             <%= link_to 'My Blog', root_path, class: 'nav-link js-scroll-trigger' %>
             <%= link_to 'New Article', new_article_path, class: 'nav-link js-scroll-trigger' %>
             <%= link_to 'Back', root_path, class: 'nav-link js-scroll-trigger' %>   
        </div>
    </nav>

    <!-- Header -->
    <header class="masthead">
      <section id="articles" class="container my-5">
        <h1>Listing Articles</h1>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Text</th>
                    <th colspan="3">Actions</th>
                </tr>
            </thead>
            <tbody>
            <% @articles.each do |article| %>
                    <tr>
                        <td><%= article.title %></td>
                        <td><%= article.text %></td>
                        <td><%article.user.email%>
                        <td><%= link_to 'Show', article_path(article), class: 'btn btn-info btn-sm' %></td>
                        <%if article.user.id == current_user.id %>
                            <td><%= link_to 'Edit', edit_article_path(article), class: 'btn btn-warning btn-sm' %></td>
                            <td><%= button_to 'Destroy', article_path(article), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm' %></td>
                        <%end%>
                    </tr>
            <% end %> 
            </tbody>
        </table>
    </section>
  
    </header>

    <!-- Article List Section -->
    
    <!-- Footer -->
    <footer class="text-center">
        <div class="footer-below">
            <div class="container">
                <div class="col-lg-12">
                    <h3>Ingeniería en Telemática</h3>
                    <ul class="list-inline">
                        <li class="list-inline-item">
                            <a class="btn-social btn-outline" href="#">
                                <i class="fa fa-fw fa-facebook"></i>
                            </a>
                        </li>
                        <li class="list-inline-item">
                            <a class="btn-social btn-outline" href="#">
                                <i class="fa fa-fw fa-twitter"></i>
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </footer>

    <!-- Scroll to Top Button (Only visible on small and extra-small screen sizes) -->
    <div class="scroll-top d-lg-none">
        <a class="btn btn-primary js-scroll-trigger" href="">
            <i class="fa fa-chevron-up"></i>
        </a>
    </div>

    <!-- Bootstrap core JavaScript -->
    <script src="assets/jquery.min.js"></script>
    <script src="assets/bootstrap.bundle.min.js"></script>
    <!-- Contact Form JavaScript -->
    <script src="assets/jqBootstrapValidation.js"></script>
    <!-- Custom scripts for this template -->
    <script src="assets/freelancer.min.js"></script>
</body>

</html>
